function sum(x, y, z){
    let result = x.length + y.length + z.length;
    let avg = Math.floor(result/3);
    console.log(result);
    console.log(avg);
}

sum ('chocolate', 'ice cream', 'cake')

